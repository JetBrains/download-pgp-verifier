buildscript {
    ext.kotlin_version = '1.8.20'

    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

apply plugin: 'java'
apply plugin: 'kotlin'
apply plugin: 'maven-publish'

repositories {
    mavenCentral()
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
}

dependencies {
    implementation 'org.bouncycastle:bcpg-jdk18on:1.71'
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"

    def junitVersion = "5.7.2"
    testImplementation "org.junit.jupiter:junit-jupiter-params:$junitVersion"
    testImplementation "org.junit.jupiter:junit-jupiter-api:$junitVersion"
    testRuntimeOnly "org.junit.jupiter:junit-jupiter-engine:$junitVersion"
}

apply plugin: 'maven-publish'

java {
    withSourcesJar()
}

test {
    useJUnitPlatform()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = 'com.jetbrains.infra'
            artifactId = 'download-pgp-verifier'
            version = "1.1.$System.env.JB_SPACE_EXECUTION_NUMBER"

            from components.java

            pom {
                url = 'https://github.com/JetBrains/download-pgp-verifier'

                licenses {
                    license {
                        name = 'The Apache License, Version 2.0'
                        url = 'https://www.apache.org/licenses/LICENSE-2.0.txt'
                    }
                }
            }
        }
    }

    repositories {
        maven {
            credentials {
                username = "$System.env.INTELLIJ_DEPENDENCIES_PUBLICATION_CLIENT_ID"
                password = "$System.env.INTELLIJ_DEPENDENCIES_PUBLICATION_CLIENT_SECRET"
            }

            url "https://packages.jetbrains.team/maven/p/ij/intellij-dependencies"
        }
    }
}

wrapper {
    gradleVersion = '7.2'
    distributionUrl = "https://cache-redirector.jetbrains.com/services.gradle.org/distributions/gradle-${gradleVersion}-bin.zip"
}
